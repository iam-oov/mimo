[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uv run uvicorn src.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/calculator"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
